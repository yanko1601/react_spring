import './Profile.css'

const Profile = (props) => {

    return(
        <div>
            {console.log(props)}
            <h2>Profile component</h2>
        </div>
    );
}

export default Profile;